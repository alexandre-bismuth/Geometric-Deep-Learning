experiment_id: zinc-vnode

model:
  hidden_dim: 64
  num_heads: 4

architecture:
  num_layers: 10
  mpnn: gine
  task: regression

pe:
  type: rwse
  dim: 28

vnode:
  enabled: true
  num_node_types: 29
  num_edge_types: 5
  vnode_node_type: 28
  vnode_edge_type: 4

data:
  dataset: zinc
  batch_size: 32

training:
  epochs: 1500
  warmup_epochs: 50
  lr: 0.001
  weight_decay: 1.0e-5
  grad_clip: 1.0
