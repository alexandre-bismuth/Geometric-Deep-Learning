experiment_id: pascal-grit

model:
  hidden_dim: 96
  num_heads: 4
  attn_dropout: 0.5
  dropout: 0.0
  pool: mean

architecture:
  num_layers: 4
  task: node_classification

pe:
  type: rrwp
  dim: 17

data:
  dataset: pascal_voc_sp
  batch_size: 32
  num_workers: 4

training:
  epochs: 300
  warmup_epochs: 5
  lr: 0.0005
  weight_decay: 0.0
  grad_clip: 1.0
